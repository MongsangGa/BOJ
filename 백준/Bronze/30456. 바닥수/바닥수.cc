#include <bits/stdc++.h>

using namespace std;

int n, p;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cin >> n >> p;
    for (int i = 0; i < p - 1; i++) cout << "1";
    cout << n;
    return 0;
}

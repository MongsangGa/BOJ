#include <bits/stdc++.h>

using namespace std;

#define fastio cin.tie(0), ios_base::sync_with_stdio(0)

int n;

int main() {
    fastio;
    cin >> n;
    if (n % 2) cout << "CY";
    else cout << "SK";
    return 0;
}
